{
  "name": "memory-storage-check",
  "version": "0.1.0",
  "description": "Manual smoke check for the memory-storage layer (real embeddings)",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsx src/check.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "memory-storage": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
